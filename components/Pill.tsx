export const Pill = ({ children }: { children: React.ReactNode }) => (
    <span className="rounded-lg border border-black/20 bg-other px-2 py-0.5 font-display text-xs text-slate-900">
        {children}
    </span>
)
